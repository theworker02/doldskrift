# Research: machine-readable writing

Phase II advances the thesis that glyphs can carry recoverable identity through geometry (zones, fingerprints, redundancy) without machine learning.

Open questions: optimal bit allocation under blur/JPEG; token-boundary symbols in live agent streams; variable-font axes for density/redundancy.
