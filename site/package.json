{
  "name": "doldskrift-site",
  "private": true,
  "version": "0.1.0",
  "description": "Static multi-page GitHub Pages site (Vite build → site/dist)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node -e \"process.exit(0)\""
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
